# Pinned versions used to produce the published benchmark results.
# Tested on Apple M4 Pro (macOS 15), Python 3.12.
# Install: pip install -r requirements.txt

mlx==0.29.3
jax==0.4.30
jaxlib==0.4.30
numpy==2.0.2
